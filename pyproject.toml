[project]
dependencies = [
  "colorama",
  "pywin32 ; platform_system == \"Windows\"",
]
name = "rpycli"
version = "0.0.3"

[project.optional-dependencies]
dev = [
  "mypy",
  "pyright",
  "pytest",
]

[tool.mypy]
packages = [
  "rpycli",
]
strict = false
